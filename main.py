import ast
import json
import math
from urllib.parse import urlencode

import demjson as demjson
import re

import yaml
from requests_html import HTML, HTMLSession
from scrapy import Selector
from scrapy.cmdline import execute

# execute(['scrapy', 'crawl', 'ifeng'])
# execute(['scrapy', 'crawl', 't'])
execute(['scrapy', 'crawl', 'pv'])
# for p in range(2, int(math.ceil(45 / 20)) + 1):
#     print(p)
x = r''''''
d = {}


# '''
def fun():
    pass
